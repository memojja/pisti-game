package model;

public class Smartbot extends Player {
    public Smartbot(int index) {
        super(index);
    }

    @Override
    public Card logic(GameState gameState) {
        return null;
    }
}
